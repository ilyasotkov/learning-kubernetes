apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: {{ template "nginx-release.name" . }}
spec: # ServiceSpec
  ports:
    - port: 80
  type: ClusterIP
  selector:
    app: {{ template "nginx-release.name" . }}
